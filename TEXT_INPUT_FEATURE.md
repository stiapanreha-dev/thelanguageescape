# Тип задания "text_input" - Ввод текста

## Описание
Новый тип задания, который позволяет пользователю вводить текст в чат с валидацией.

## Формат JSON

```json
{
  "task_number": 2,
  "type": "text_input",
  "title": "Введи своё имя",
  "question": "🎤 Терминал: \"State your identity. Type your name.\"",
  "instruction": "Введите своё имя в чат и отправьте сообщение.",
  "validation_pattern": "name",
  "placeholder": "Например: John",
  "correct_message": "✅ Отлично, {user_input}! Имя сохранено.",
  "incorrect_message": "❌ Имя должно содержать только буквы и быть длиной от 2 до 50 символов.",
  "hints": [
    "💡 Введите ваше настоящее имя",
    "💡 Можно использовать только буквы (русские или английские)"
  ]
}
```

### Параметры

#### Обязательные:
- **`type`**: `"text_input"` - тип задания
- **`question`**: Основной вопрос/инструкция для пользователя

#### Опциональные:
- **`title`**: Заголовок задания (для логов)
- **`instruction`**: Дополнительная инструкция (показывается под вопросом)
- **`placeholder`**: Пример ввода (показывается как подсказка)
- **`validation_pattern`**: Тип валидации (см. ниже)
- **`correct_message`**: Сообщение при успешном вводе
  - Поддерживает `{user_input}` - будет заменено на введённый текст
- **`incorrect_message`**: Сообщение при ошибке валидации
- **`hints`**: Массив подсказок (показываются по кнопке "Подсказка")

## Типы валидации (validation_pattern)

### 1. `"name"` - Имя пользователя
- Только буквы (русские или английские) и пробелы
- Длина: 2-50 символов
- Примеры: `John`, `Иван Петров`, `Mary Jane`

**Regex:** `^[a-zA-Zа-яА-ЯёЁ\s]{2,50}$`

### 2. `"email"` - Email адрес
- Базовая валидация email
- Примеры: `user@example.com`, `test.user@mail.ru`

**Regex:** `^[\w\.-]+@[\w\.-]+\.\w+$`

### 3. `"number"` - Только цифры
- Только цифры, без букв и знаков
- Примеры: `123`, `42`, `1000`

### 4. `"any"` или `null` - Любой текст
- Проверяется только что текст не пустой
- Подходит для свободного ввода

## Примеры использования

### Пример 1: Ввод имени (валидация "name")

```json
{
  "task_number": 2,
  "type": "text_input",
  "question": "🎤 Терминал: \"State your identity. Type your name.\"",
  "instruction": "Введите своё имя в чат.",
  "validation_pattern": "name",
  "placeholder": "John",
  "correct_message": "✅ Записано, {user_input}. Система вас запомнила.",
  "incorrect_message": "❌ Имя должно содержать только буквы.",
  "hints": [
    "💡 Введите ваше настоящее имя",
    "💡 Используйте только буквы"
  ]
}
```

**Поведение:**
- Пользователь видит вопрос и инструкцию
- Вводит имя в чат (например, "Victor")
- Если валидация прошла → сообщение "✅ Записано, Victor. Система вас запомнила."
- Автоматический переход на следующее задание

### Пример 2: Ввод email (валидация "email")

```json
{
  "task_number": 5,
  "type": "text_input",
  "question": "📧 Введите ваш email для связи",
  "validation_pattern": "email",
  "placeholder": "your.email@example.com",
  "correct_message": "✅ Email {user_input} сохранён!",
  "incorrect_message": "❌ Некорректный формат email. Попробуйте снова."
}
```

### Пример 3: Свободный ввод (без валидации)

```json
{
  "task_number": 3,
  "type": "text_input",
  "question": "💬 Опишите, что вы видите на экране",
  "instruction": "Напишите своими словами.",
  "validation_pattern": "any",
  "correct_message": "✅ Ответ принят: \"{user_input}\"",
  "hints": [
    "💡 Опишите детали",
    "💡 Можно писать всё, что думаете"
  ]
}
```

### Пример 4: Ввод числа (валидация "number")

```json
{
  "task_number": 7,
  "type": "text_input",
  "question": "🔢 Сколько символов в коде доступа?",
  "validation_pattern": "number",
  "placeholder": "10",
  "correct_message": "✅ Число {user_input} записано.",
  "incorrect_message": "❌ Введите только цифры."
}
```

## Интерфейс

Когда отображается text_input задание, пользователь видит:

```
**🎤 Терминал: "State your identity. Type your name."**

Введите своё имя в чат и отправьте сообщение.

_Пример: John_

┌─────────────────────┐
│  💡 Подсказка       │
└─────────────────────┘
┌─────────────────────┐
│  ⏭ Пропустить       │
└─────────────────────┘
```

После ввода:
- ✅ Если валидация прошла → показывается `correct_message` и переход на следующее задание
- ❌ Если валидация не прошла → показывается `incorrect_message`, задание остаётся

## Сохранение данных

Введённый текст сохраняется в БД:
- **Таблица:** `TaskResult`
- **Поле:** `user_answer` - введённый пользователем текст
- **is_correct:** `True` (если прошла валидация)

### Использование введённого имени в других заданиях

Система автоматически подставляет имя из **День 1, Задание 2** (или любого другого text_input с `validation_pattern: "name"`) вместо плейсхолдера `[Имя]` в текстах заданий.

**Пример:**
```json
{
  "task_number": 3,
  "type": "choice",
  "question": "Эмма: \"Nice to meet you, [Имя]! Where are you from?\"",
  "options": [
    "A) I'm from Russia",
    "B) I'm from USA"
  ]
}
```

Если пользователь ввёл имя "Victor" в задании 2, то в задании 3 увидит:
```
Эмма: "Nice to meet you, Victor! Where are you from?"
```

## Кнопки

### 💡 Подсказка
- Показывает подсказки из массива `hints` по очереди
- Callback: `hint_{day}_{task}`

### ⏭ Пропустить
- Пропускает задание (сохраняет `user_answer: "SKIPPED"`)
- Callback: `skip_task_{day}_{task}`

## Auto-transition

После успешного ввода текста (прошла валидация):
- Автоматический переход на следующее задание
- Как в голосовых заданиях (voice tasks)

## Сравнение с другими типами

| Тип | Ввод | Валидация | Auto-transition |
|-----|------|-----------|----------------|
| `choice` | Кнопки | Да (правильный ответ) | Да (если настроено) |
| `voice` | Голос | Да (ключевые слова) | Да |
| `text_input` | Текст | Да (pattern) | Да |
| `audio` | Кнопки | Да (правильный ответ) | Да (если настроено) |

## Реализация

### Файлы:
- **`bot/handlers/tasks.py`**:
  - Добавлен `TaskStates.waiting_for_text_input`
  - Обработчик `elif task_type == 'text_input'` в `show_task()`
  - Handler `@router.message(F.text, TaskStates.waiting_for_text_input)`
  - Функция `validate_text_input(text, pattern)`

### Логика:
1. При показе text_input задания → устанавливается `state = waiting_for_text_input`
2. Пользователь вводит текст → вызывается `handle_text_input()`
3. Валидация через `validate_text_input()`
4. Если валидация OK → сохранение в БД + переход на следующее задание
5. Если валидация FAIL → показывается `incorrect_message`

## Тестовый JSON для Дня 1, Задание 2

Замените голосовое задание на text_input:

```json
{
  "day": 1,
  "title": "Day 1: First Contact",
  "tasks": [
    {
      "task_number": 1,
      "type": "choice",
      "question": "Выберите правильный вариант",
      "options": ["A) Yes", "B) No"],
      "correct_answer": "A"
    },
    {
      "task_number": 2,
      "type": "text_input",
      "question": "🎤 Терминал: \"State your identity. Type your name.\"",
      "instruction": "Введите своё имя в чат.",
      "validation_pattern": "name",
      "placeholder": "John",
      "correct_message": "✅ Отлично, {user_input}! Система вас запомнила.",
      "incorrect_message": "❌ Имя должно содержать только буквы (2-50 символов).",
      "hints": [
        "💡 Введите ваше настоящее имя",
        "💡 Можно использовать русские или английские буквы"
      ]
    },
    {
      "task_number": 3,
      "type": "choice",
      "block": 1,
      "question": "Эмма: 'Hi, [Имя]! Ready to continue?'",
      "options": ["A) Yes!", "B) Wait"],
      "correct_answer": "A"
    }
  ]
}
```

## Дата реализации
2025-11-08
